/**
 * Re-export the SchemaStore interface from core types.
 * This file exists for organizational clarity — custom store implementations
 * should implement the SchemaStore interface from '../core/types'.
 */

export { SchemaStore, SchemaSnapshot } from '../core/types';

